name: Tests on PR

on: pull_request

jobs:
  unit_tests:
    runs-on: ubuntu-latest
    container: node:18-alpine
    steps:
      - name: Install deps
        run: yarn --frozen-lockfile

      - name: Jest CI
        run: yarn test-ci
