name: Tests on PR
on: pull_request
jobs:
  unit_tests:
    runs-on: ubuntu-latest
    container: node:18-alpine
    steps:
      - name: Install deps
        run: yarn --frozen-lockfile

      - run: ls

      - name: Jest CI
        uses: actions/checkout@v3
        run: yarn test-ci
