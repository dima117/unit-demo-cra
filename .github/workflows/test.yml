name: Tests on PR
on: pull_request
jobs:
  unit_tests:
    runs-on: ubuntu-latest
    container: node:18-alpine
    steps:
      - uses: actions/checkout@v3
      - run: npm ci
      - run: npm run test-ci
