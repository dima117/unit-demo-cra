name: CI

on: [push, pull_request]

jobs:
  commitlint:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
      - name: Print versions
        run: |
          git --version
          node --version
          npm --version
          npx commitlint --version
      - name: Install commitlint
        run: |
          npm install conventional-changelog-conventionalcommits
          npm install commitlint@latest

      - name: Validate pushed commits with commitlint
        if: github.event_name == 'push'
        run: |
          echo "🐧 Хэши первого и последнего коммитов в пуше  ${{ github.event.before }} и ${{ github.event.after }}"
          npx commitlint --from ${{ github.event.before }} --to HEAD --verbose

      - name: Validate PR commits with commitlint
        if: github.event_name == 'pull_request'
        run: |
          echo "🐭 Хэши проверяемых коммитов в ПР  ${{ github.event.pull_request.base.sha }}  и ${{ github.event.pull_request.head.sha }} "
          npx commitlint --from ${{ github.event.pull_request.base.sha }} --to ${{ github.event.pull_request.head.sha }} --verbose
