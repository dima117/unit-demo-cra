name: CI

on:
  push:
    branches: [main, PR]
jobs:
  verify:
    name: Verify Files
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .
    steps:
    - name: Install packages
      run: npm i
    - name: tests
      run: npm test
    - name: Lint
      run: eslint ./src/Application.tsx