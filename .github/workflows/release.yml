name: Release

on:
  push:
    tags:
      - v\d+

permissions:
  issues: write
  contents: write

jobs:
  review:
    uses: ./.github/workflows/review-changes.yml
  deploy:
    uses: ./.github/workflows/deploy.yml
    needs: review
  changelog:
    uses: ./.github/workflows/changelog.yml
    if: ${{ always() }}
    needs:
      - review
      - deploy
    with:
      release: ${{ github.ref_name }}
      review-verified: ${{ needs.review.result == 'success' }}
      review-url: ${{ needs.review.outputs.url }}
      deploy-verified: ${{ needs.deploy.outputs.deploy-status == 'success' }}
      deploy-url: ${{ needs.deploy.outputs.workflow-url }}
      pages-url: ${{ needs.deploy.outputs.paged-url }}
